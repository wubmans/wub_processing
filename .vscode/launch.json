{
    "configurations": [
        {
            "type": "firefox",
            "request": "attach",
            "name": "Attach",
            "pathMappings": [
                {
                    "url": "http://localhost:5500",
                    "path": "${workspaceFolder}"
                },
                {
                    "url": "http://localhost:5500",
                    "path": "${workspaceFolder}"
                }
            ]
        }
    ]
}